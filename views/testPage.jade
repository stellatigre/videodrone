include index
html
    link(rel='stylesheet', href='//cdnjs.cloudflare.com/ajax/libs/mocha/1.12.1/mocha.min.css')
  body
    #mocha
    script(src='//cdnjs.cloudflare.com/ajax/libs/mocha/1.12.1/mocha.min.js')
    script(src='//chaijs.com/chai.js')
    script
      var mocha;
      mocha.ui('bdd');
      var expect = chai.expect;
    script(src='browser_tests.js')
    script
      if (window.mochaPhantomJS) { mochaPhantomJS.run(); }
      else { mocha.run(); }

